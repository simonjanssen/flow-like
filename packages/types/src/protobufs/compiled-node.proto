syntax = "proto3";

package flow_like_types;

import "pin.proto";
import "variable.proto";

message CompiledNode {
    string id = 1;
    string name = 2;
    map<string, Pin> pins = 3;
    bool start = 4;
}